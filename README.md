A collection of neat bash tricks.

Some random useful tidbits:

1) This

    $ mkdir -p new/folder/structure
    $ cd new/folder/structure
    
is equivalent to

    $ mkdir -p new/folder/structure
    $ cd !$
